Tache 5 : Documents techniques

Que commande Linux cat représenter ? concatenate

Qu'est-ce que le netstat paramètre dans MS Windows qui affiche l'exécutable associé à chaque connexion active et port d'écoute ? -b